machine:
  java:
    version: openjdk8
  services:
    - docker:
      version: 17.10.0-ce
    
test:
  override:
    - curl -fsSL get.nextflow.io | bash
    - ./nextflow run . -with-docker :
        timeout: 1200
